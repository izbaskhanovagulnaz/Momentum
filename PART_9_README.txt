Fixed iPhone calendar input focus: the task title and time fields no longer remount after every character/change, so the keyboard and time picker stay open.

Run deploy.bat. After deployment, use the in-app update prompt or fully close and reopen the PWA once.
