PART 10 - TASK EDITING AND PRIORITIES

Added:
- edit tasks from Calendar;
- edit title, date, time and priority;
- three priority levels: high, medium and low;
- improved Tasks page with summary cards, filters and search;
- task cards show date, time and priority;
- edit tasks directly from Tasks page;
- Firestore synchronization for all changes.

Run deploy.bat. In the installed PWA, use the update prompt when it appears.
